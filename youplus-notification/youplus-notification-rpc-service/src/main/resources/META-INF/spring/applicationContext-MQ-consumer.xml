<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/context
	 	  http://www.springframework.org/schema/context/spring-context-4.1.xsd">

    <bean id="notificationListener" class="cn.com.youplus.notification.rpc.service.mq.listener.NotificationMessageListener" /> <!--Listener配置-->

    <bean id="consumer" class="com.aliyun.openservices.ons.api.bean.ConsumerBean" init-method="start" destroy-method="shutdown">
        <property name="properties" > <!--消费者配置信息-->
            <props>
                <prop key="ConsumerId">${aliyun.mq.consumer_id}</prop> <!--请替换XXX-->
                <prop key="AccessKey">${aliyun.mq.access_key_id}</prop>
                <prop key="SecretKey">${aliyun.mq.access_key_secret}</prop>
                <!--将消费者线程数固定为50个 -->
                <prop key="ConsumeThreadNums">20</prop>
            </props>
        </property>
        <property name="subscriptionTable">
            <map>
                <entry value-ref="notificationListener">
                    <key>
                        <bean class="com.aliyun.openservices.ons.api.bean.Subscription">
                            <property name="topic" value="${aliyun.mq.block_order_topic_name}"/>
                            <!--expression即Tag，可以设置成具体的Tag，如 taga||tagb||tagc，也可设置成*。 *仅代表订阅所有Tag，不支持通配-->
                            <property name="expression" value="${aliyun.mq.notification.tags}"/>
                        </bean>
                    </key>
                </entry>

                <entry value-ref="notificationListener">
                    <key>
                        <bean class="com.aliyun.openservices.ons.api.bean.Subscription">
                            <property name="topic" value="${aliyun.mq.none_order_topic_name}"/>
                            <!--expression即Tag，可以设置成具体的Tag，如 taga||tagb||tagc，也可设置成*。 *仅代表订阅所有Tag，不支持通配-->
                            <property name="expression" value="${aliyun.mq.notification.tags}"/>
                        </bean>
                    </key>
                </entry>
            </map>
        </property>
    </bean>

</beans>