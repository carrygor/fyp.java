<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/context
	 	  http://www.springframework.org/schema/context/spring-context-4.1.xsd">

    <bean id="broadcastMessageListener" class="cn.com.youplus.base.rpc.service.mq.listener.BroadcastMessageListener" /> <!--Listener配置-->

    <bean id="consumer" class="com.aliyun.openservices.ons.api.bean.ConsumerBean" init-method="start" destroy-method="shutdown">
        <property name="properties" > <!--消费者配置信息-->
            <props>
                <prop key="ConsumerId">${aliyun.mq.brocast_consumer_id}</prop>
                <prop key="AccessKey">${aliyun.mq.access_key_id}</prop>
                <prop key="SecretKey">${aliyun.mq.access_key_secret}</prop>
                <!--广播接收-->
                <prop key="MessageModel">BROADCASTING</prop>
                <!--将消费者线程数固定为N个 -->
                <prop key="ConsumeThreadNums">2</prop>
            </props>
        </property>
        <property name="subscriptionTable">
            <map>
                <entry value-ref="broadcastMessageListener">
                    <key>
                        <bean class="com.aliyun.openservices.ons.api.bean.Subscription">
                            <property name="topic" value="${aliyun.mq.block_order_topic_name}"/>
                            <!--expression即Tag，可以设置成具体的Tag，如 taga||tagb||tagc，也可设置成*。 *仅代表订阅所有Tag，不支持通配-->
                            <property name="expression" value="${aliyun.mq.base.broadcast_tags}"/>
                        </bean>
                    </key>
                </entry>
            </map>
        </property>
    </bean>

</beans>