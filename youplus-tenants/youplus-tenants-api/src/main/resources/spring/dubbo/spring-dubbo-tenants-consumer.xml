<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://code.alibabatech.com/schema/dubbo 
		http://code.alibabatech.com/schema/dubbo/dubbo.xsd
		http://www.springframework.org/schema/beans  
		http://www.springframework.org/schema/beans/spring-beans.xsd 
		http://www.springframework.org/schema/context 
	 	http://www.springframework.org/schema/context/spring-context-4.1.xsd">

	<dubbo:reference  id="upTenantsCompanyService"
					interface="cn.com.youplus.tenants.api.auto.UpTenantsCompanyService"
					version="${dubbo.interface.version}"/>

	<dubbo:reference  id="upTenantsLevelService"
					interface="cn.com.youplus.tenants.api.auto.UpTenantsLevelService"
					version="${dubbo.interface.version}"/>

	<dubbo:reference  id="upTenantsRegionService"
					interface="cn.com.youplus.tenants.api.auto.UpTenantsRegionService"
					version="${dubbo.interface.version}"/>

	<dubbo:reference  id="upTenantsRequestLogService"
					interface="cn.com.youplus.tenants.api.auto.UpTenantsRequestLogService"
					version="${dubbo.interface.version}"
	/>

	<dubbo:reference  id="upTenantsUserRoleService"
					  interface="cn.com.youplus.tenants.api.auto.UpTenantsUserRoleService"
					  version="${dubbo.interface.version}"/>

	<dubbo:reference  id="upTenantsUserService"
					  interface="cn.com.youplus.tenants.api.auto.UpTenantsUserService"
					  version="${dubbo.interface.version}"/>

	<dubbo:reference  id="dataStructureService"
					  interface="cn.com.youplus.tenants.api.common.DataStructureService"
					  version="${dubbo.interface.version}" />

	<dubbo:reference  id="upTenantsUserRegionViewService"
					  interface="cn.com.youplus.tenants.api.auto.UpTenantsUserRegionViewService"
					  version="${dubbo.interface.version}" />

	<dubbo:reference  id="upTenantsTaskService"
					interface="cn.com.youplus.tenants.api.auto.UpTenantsTaskService"
					version="${dubbo.interface.version}"/>

</beans>
